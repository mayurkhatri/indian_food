<h1>Recipe List <%= @recipeable %></h1>
<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>    
  </tr>
<% unless @recipes.blank? %>  
  <% @recipes.each do |recipe| %>  
    <tr>
      <td><%= recipe.name %></td>
      <td><%= link_to 'Show', course_recipe_path(@course, recipe)  %></td>
      <td><%= link_to 'Edit', edit_course_recipe_path(@course, recipe) %></td>        
      <td><%= link_to 'Delete', courses_path(@course, recipe) %></td>
    </tr>
  <% end %>
<% end %>
</table>

<br/>

<%= link_to 'New', new_course_recipe_path(@course) %>
