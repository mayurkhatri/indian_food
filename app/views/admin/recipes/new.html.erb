<h1><%= t('admin.recipe.new_recipe')%></h1>

<%= render 'form', :recipe => @recipe, :recipeable => @recipeable, :course_name => @course_name %>

<%= link_to t('generic.back'), polymorphic_path([:admin, @recipeable, Recipe.new]) %>
