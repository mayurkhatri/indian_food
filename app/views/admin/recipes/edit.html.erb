<h1>Edit Recipe</h1>

<%= render 'form', :recipe => @recipe, :recipeable => @recipeable %>

<%= link_to 'Show', polymorphic_path([:admin, @recipeable,@recipe]) %> |
<%= link_to 'Back', polymorphic_path([:admin, @recipeable, Recipe.new]) %>

